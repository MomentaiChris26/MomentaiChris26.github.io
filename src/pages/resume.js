import * as React from "react";
import DefaultLayout from "../components/layout";

const Resume = () => {
  return (
    <DefaultLayout>
      <h1>Resume</h1>
    </DefaultLayout>
  );
};

export default Resume;
