import * as React from "react";
import DefaultLayout from "../components/layout";

const IndexPage = () => {
  return (
    <DefaultLayout>
      <div>Title</div>
    </DefaultLayout>
  );
};

export default IndexPage;
